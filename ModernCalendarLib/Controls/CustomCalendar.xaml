<UserControl x:Class="ModernCalendarLib.CustomCalendar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="Transparent">
        <Border  CornerRadius="5" >
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <DockPanel Grid.Row="0" Margin="0 0 0 10">

                    <Grid>
                        <Button Style="{DynamicResource LeftNavigation}" HorizontalAlignment="Left" Content="◀" Click="PrevMonth_Click"/>
                        <TextBlock x:Name="MonthText" Style="{DynamicResource MonthText}" HorizontalAlignment="Center" VerticalAlignment="Center" MouseDown="MonthText_MouseDown" Cursor="Hand"/>
                        <Button Content="▶" Style="{DynamicResource RightNavigation}" HorizontalAlignment="Right" Click="NextMonth_Click"/>
                    </Grid>
                   
                </DockPanel>

                <!-- Day Headers -->
                <UniformGrid Grid.Row="1" Columns="7" Margin="0 0 0 5" x:Name="DayHeadersPanel">
                    <TextBlock Text="Mon" Style="{DynamicResource DayHeaderText}"/>
                    <TextBlock Text="Tue" Style="{DynamicResource DayHeaderText}"/>
                    <TextBlock Text="Wed" Style="{DynamicResource DayHeaderText}"/>
                    <TextBlock Text="Thu" Style="{DynamicResource DayHeaderText}"/>
                    <TextBlock Text="Fri" Style="{DynamicResource DayHeaderText}"/>
                    <TextBlock Text="Sat" Style="{DynamicResource DayHeaderText}"/>
                    <TextBlock Text="Sun" Style="{DynamicResource DayHeaderText}"/>
                </UniformGrid>


                <!-- Days -->
                <UniformGrid x:Name="DaysGrid" Grid.Row="2" Columns="7"/>

                <!-- Year Picker -->
                <UniformGrid x:Name="YearGrid" Grid.Row="2" Columns="4" Visibility="Collapsed"/>
            </Grid>
        </Border>
    </Grid>
    
</UserControl>
